第一次建立环境

git init

git add .

git config --global user.email "rcdrones@163.com"

git config --global user.name "rcdrones"

git commit -m "first commit"

git remote add origin https://github.com/rcdrones/gittest.git

git push -u origin master


--------------------------

以后就是：

git add .

git commit -m "first commit"

git push -u origin master  